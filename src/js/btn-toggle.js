export function loadBtnOff(loadMoreBtnEl) {
  loadMoreBtnEl.classList.add('is-hidden');
}

export function loadBtnOn(loadMoreBtnEl) {
  loadMoreBtnEl.classList.remove('is-hidden');
}

export function scrollBtnOff(upScrollBtnEl) {
  upScrollBtnEl.classList.add('is-hidden');
}

export function scrollBtnOn(upScrollBtnEl) {
  upScrollBtnEl.classList.remove('is-hidden');
}
